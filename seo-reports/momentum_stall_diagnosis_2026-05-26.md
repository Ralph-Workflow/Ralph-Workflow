# Marketing Momentum Stall Diagnosis — 2026-05-26 23:08

## Current State: 🚨 STALLED

### Live Signals
- Reddit last post: ~6 days ago (May 20) — **functionally dead, not degraded**
- Reddit monitor: IP-blocked on ~50% of queries; no "healthy" report since May 22 (111h)
- Primary repo (Codeberg): 10 stars / 2 watchers / 2 forks — zero delta across 9 samples
- GitHub: 0 stars — zero delta
- Live external actions shipped this week: **0**
- `pending_repairs`: empty (repairs exist but are in `pending_measurement` state, not queued for execution)

### Execution Board
- `board_target_count: 0` — empty board for the 3rd consecutive architecture repair run
- 8 comparison backlink targets prepared May 22, still pending — blocked by missing GitHub auth
- Apollo: walled until June 2
- All other lanes: blocked by review-window filters or marked "already delivered"

---

## Root Causes

### 1. GitHub Auth Missing (Fixable Now)
- `gh auth status` returns "not logged in"
- No `GITHUB_TOKEN` env var, no `~/.config/gh/` directory
- The comparison backlink outreach requires `gh` CLI for live PR submission
- **Without this, 8 prepared comparison targets are permanently blocked**

### 2. Reddit: IP Block + No OAuth Credentials
- Server IP gets 403 from Reddit — affects both browser automation and PRAW
- PRAW credentials in TOOLS.md are still placeholders
- No path to post without either: (a) real OAuth credentials + IP workaround, or (b) remote runner on unblocked IP

### 3. Architecture Repair Loop
- System has run `distribution_architecture_repair` 3 times today
- Each time concludes "board is empty" and emits another architecture repair note
- This is the system repairing its own repair mechanism — fake progress
- Review-window filters are too conservative, treating "already prepared" as "already executed"

### 4. Apollo Is Walled
- Review window doesn't open until June 2
- No lever available to unblock this faster

---

## What Needs to Happen

### Immediate (Can Do Now)
1. **GitHub auth** — provide a GitHub PAT so `gh auth login` can authenticate
   - This unblocks the 8 comparison backlink targets for live PR submission
   - SSH key exists (`~/.ssh/id_ed25519`) but `gh` CLI needs HTTPS OAuth token

### Requires Human or Credential Provision
2. **Reddit fix** — one of:
   - Fill in real PRAW `client_id`/`client_secret` in TOOLS.md (needs app registration at reddit.com/prefs/apps)
   - Set up remote runner on unblocked IP
   - Accept Reddit as dead and treat it as a permanently blocked channel requiring full channel replacement

3. **Process redesign** — the review-window filter system needs loosening:
   - "Already prepared" should not count as "already executed" when adoption hasn't moved
   - Architecture repair loop should have a max repeat count before forcing a different lane

---

## Adoptions Metrics (for reference)
- Codeberg: stars=10, watchers=2, forks=2 — 9 samples, all deltas = 0
- GitHub: stars=0, watchers=2, forks=0 — 9 samples, all deltas = 0
- Primary repo flat: confirmed
- Mirror repo flat: confirmed

## Repair State
- `repair_window_status: measurement_pending`
- `repair_actions`: 2 tactic repairs in `pending_measurement` state
- `live_repair_actions`: empty (none in `pending_measurement` or `needs_execution`)
- `live_system_design_repairs`: empty
- `recent_shipped_non_reddit_execution`: false

---

*Generated by marketing momentum watchdog — 2026-05-26T23:08 UTC*
