# Ralph Workflow (Python)

Python 3.14 implementation of the Ralph Workflow CLI.

## Development

Install development dependencies:

```bash
pip install -e ".[dev]"
```

Run verification:

```bash
make verify
```

This package exposes the `ralph` CLI via `ralph.cli.main:app`.
