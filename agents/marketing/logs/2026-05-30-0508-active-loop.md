# Marketing Active Loop — 2026-05-30 05:08 UTC Run
**Hold window active** (clears 2026-05-30T08:36:38 UTC, ~1.5h remaining)

## Action Taken: 3 Concrete Repairs During Measurement Hold

### 1. JSON Feed feed_url Spec Compliance (commit `7a431af`, release `20260530054141`)
**Bug**: `feed_url` was `https://ralphworkflow.com/blog/feed` — the RSS endpoint. JSON Feed 1.1 spec requires `feed_url` to be the feed's own URL for AI crawler refresh and unique identification. With the wrong URL, crawlers can't tell whether they've already indexed this feed.
**Fix**: Changed `json.feed_url blog_feed_json_url` to `json.feed_url "#{blog_feed_json_url}.json"` — now correctly resolves to `https://ralphworkflow.com/blog/feed.json`.

### 2. Codeberg Link Coverage: 35/36 → 36/36 (commit `0bd58fc`)
**Bug**: `debugging-failed-overnight-ai-coding-run.md` had zero in-body Codeberg links (only footer CTA had them).
**Fix**: Added two Codeberg links in closing paragraph — "Ralph Workflow" linked to Codeberg, plus "Inspect the workflow on Codeberg →" callout.

### 3. Deploy IndexNow Task Path Fix (commit `0bd58fc`)
**Bug**: `lib/capistrano/tasks/runtime_fidelity.rake` used relative path `../agents/marketing/indexnow_notify.py` — this resolved to `/home/mistlight/ralph-site/../agents/marketing/` which doesn't exist. Every deploy's IndexNow step failed silently.
**Fix**: Changed to absolute path `/home/mistlight/.openclaw/workspace/agents/marketing/indexnow_notify.py`.
**Manual IndexNow**: 91 URLs submitted successfully post-deploy.

## Why These Matter
The JSON Feed fix is the highest-leverage one — our 36-post feed (346KB) is now properly spec-compliant for AI crawler auto-discovery and refresh. Broken `feed_url` was silently confusing every AI crawler trying to determine feed identity.

## Post-Hold Rerun Readiness
- JSON Feed is spec-compliant ✅
- All 36 blog posts have Codeberg conversion links ✅
- Deploy pipeline's IndexNow step will work on next deploy ✅
- Feed auto-discovery tags present on all blog post pages ✅
- Telegraph pipeline healthy (all 35 posts cross-posted) ✅
- llms.txt serving correctly with Codeberg-primary links ✅

## State
- Codeberg: 12 stars, 2 forks, 2 watchers (flat in active window)
- distribution_lane: all lanes blocked during hold
- Apollo: still Cloudflare-blocked, cannot execute
- StackOverflow: packet already delivered this review window
