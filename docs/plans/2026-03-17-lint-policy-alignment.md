# Lint Policy Alignment Implementation Plan

Ralph Workflow is a free and open-source AI agent orchestrator built around a simple Ralph-loop core.
That simple core composes into a stronger workflow system for serious repo work, and the default workflow is already strong enough to start with before you customize anything.


> **STATUS:** This plan is outdated. All custom lints have been consolidated into `ralph_lints` for performance reasons. The individual lint crates (file_too_long, forbid_mut_binding, forbid_imperative_loops, forbid_mutating_receiver_methods, forbid_interior_mutability) have been removed.

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Align workspace lint configuration, crate-root lint documentation, and custom dylints with the repository style guide so enforcement matches the documented architecture instead of accidental historical drift.

**Architecture:** Keep boundary-sensitive functional rules in dylint, keep broad Clippy policy focused on universally safe style constraints, and document every allowed exception where contributors actually discover the lint. Reduce over-strict enforcement where it contradicts `docs/code-style/*`, and add missing cargo/crate coverage for owned crates.

**Tech Stack:** Rust, Cargo workspace lints, Clippy, Dylint, repository style-guide docs

---

### Task 1: Document the style-guide-backed lint contract

**Files:**
- Modify: `CODE_STYLE.md`
- Modify: `docs/code-style/testing.md`
- Modify: `docs/tooling/dylint.md`

**Step 1: Write the failing test**

Add or update a focused doc-facing assertion in the relevant lint crate tests only if a code-path change needs executable coverage; otherwise treat this task as documentation prep for later code-backed tasks.

**Step 2: Run test to verify it fails**

Run only the targeted lint-crate tests added for later tasks.

**Step 3: Write minimal implementation**

- Add an explicit "documented exceptions" section to the style guide for the owned crates that intentionally differ from normal library/test rules.
- Update `docs/tooling/dylint.md` so its severity/placement guidance matches the actual lint design.

**Step 4: Run test to verify it passes**

Re-run the targeted lint-crate tests if this task introduced any.

### Task 2: Fix `file_too_long` so enforcement matches the style guide

> **Note:** `file_too_long` is now in `lints/ralph_lints/src/file_too_long.rs`

**Files:**
- Modify: `lints/ralph_lints/src/file_too_long.rs`
- Test: `lints/ralph_lints`
- Modify: `docs/tooling/dylint.md`

**Step 1: Write the failing test**

Add unit tests around a new file-length classification helper proving that sub-1000-line files are not hard failures, while 1000+ lines still are.

**Step 2: Run test to verify it fails**

Run: `cargo +nightly test` in `lints/ralph_lints`

Expected: FAIL because the current implementation still treats 500+ lines as a deny-level violation.

**Step 3: Write minimal implementation**

- Extract a helper that classifies line counts.
- Remove the over-strict 500-line deny behavior.
- Keep autogenerated-file handling intact.
- Update the docs to describe the new enforcement precisely.

**Step 4: Run test to verify it passes**

Run: `cargo +nightly test` in `lints/ralph_lints`

Expected: PASS.

### Task 3: Standardize `clippy.toml` commentary and crate-root lint rationale

**Files:**
- Modify: `clippy.toml`
- Modify: `ralph-workflow/clippy.toml`
- Modify: `xtask/clippy.toml`
- Modify: `test-helpers/clippy.toml`
- Modify: `tests/clippy.toml`
- Modify: `ralph-gui/clippy.toml`
- Modify: `ralph-workflow/src/lib.rs`
- Modify: `ralph-workflow/src/main.rs`
- Modify: `xtask/src/main.rs`
- Modify: `test-helpers/src/lib.rs`
- Modify: `tests/integration_tests/main.rs`
- Modify: `tests/process_system_tests/main.rs`
- Modify: `tests/system_tests/main.rs`
- Modify: `ralph-gui/src/lib.rs`
- Modify: `ralph-gui/src/main.rs`

**Step 1: Write the failing test**

Use targeted source assertions in unit tests only where behavior changes. For comment-only and attribute-only changes, rely on later clippy/verify execution.

**Step 2: Run test to verify it fails**

Run the focused lint-crate tests from Task 2 first; no separate pre-change command is required for comment-only edits.

**Step 3: Write minimal implementation**

- Give every owned `clippy.toml` the same style-guide-linked rationale structure.
- Ensure empty `disallowed-types` and `disallowed-methods` are explicitly documented as intentionally delegated to dylint.
- Rewrite crate-root lint headers so each `#![deny(...)]` block explains why the rule exists, where the style guide says so, and how to fix violations.
- Add direct source pointers in `ralph-gui` files to the manifest lint policy.

**Step 4: Run test to verify it passes**

Run the relevant crate clippy commands during final verification.

### Task 4: Add missing lint inheritance for owned crates

**Files:**
- Modify: `tests/Cargo.toml`
- Modify: `xtask/Cargo.toml`
- Modify: `test-helpers/Cargo.toml`
- Modify: `ralph-workflow/Cargo.toml`

**Step 1: Write the failing test**

Treat cargo/clippy verification as the executable check: missing manifest-level lint configuration is the current failure mode for future targets and discoverability.

**Step 2: Run test to verify it fails**

Run final workspace clippy verification after the manifest changes if any conflicting lint inheritance appears.

**Step 3: Write minimal implementation**

- Add manifest-level lint sections where they close real coverage gaps for owned crates.
- Avoid duplicating boundary-aware policy in manifest lints when dylint is the correct home.

**Step 4: Run test to verify it passes**

Run the crate clippy commands referenced by `docs/agents/verification.md`.

### Task 5: Verify the full lint pipeline

**Files:**
- Modify: any files surfaced by verification failures

**Step 1: Write the failing test**

N/A beyond the targeted lint-crate tests already added.

**Step 2: Run test to verify it fails**

Run the smallest relevant commands first, then full verification:

- `cargo +nightly test` in `lints/ralph_lints`
- `cargo clippy -p ralph-workflow -p ralph-workflow-tests -p test-helpers --all-targets --all-features -- -D warnings`
- `cargo clippy -p xtask --all-targets -- -D warnings`
- `cargo clippy -p ralph-gui --all-targets -- -D warnings`
- `cargo xtask verify`

**Step 3: Write minimal implementation**

Fix any surfaced failures immediately, preserving the style-guide-first policy.

**Step 4: Run test to verify it passes**

Re-run failing commands until `cargo xtask verify` is clean.
