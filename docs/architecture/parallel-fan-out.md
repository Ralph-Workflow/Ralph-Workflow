# Parallel fan-out (moved)

This material has been consolidated into the canonical parallel-mode page at [`../ralph-workflow/docs/sphinx/parallel-mode.md`](../ralph-workflow/docs/sphinx/parallel-mode.md). The same-workspace execution model, the `allowed_directories` path-isolation mechanism, the `work_units` vs `parallel_plan` intent shapes, and the per-worker state path layout all live on that single canonical page.
