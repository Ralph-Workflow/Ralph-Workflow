# 2026-05-18

## Ralph Workflow positioning corrections
- User corrected multiple misconceptions in my Ralph Workflow framing.
- Ralph Workflow is for big, ambitious, well-specified work; not small or narrow tasks.
- A strong first run should sound like building a real side project or major product chunk, not maintenance scraps.
- Public-facing docs should emphasize result-first evaluation: what the software does now, what checks ran (especially integration tests), and what a human can verify directly; long logs are fallback evidence only.
- Ralph Workflow depends on good software engineering practices and does not replace them.
- Public docs should not talk like internal artifact or agent-to-agent handoff docs.
- Ralph Workflow should not be casually collapsed into generic "Ralph"; it is an improvement on Ralph, not the same thing.
- User explicitly clarified repo authority: Codeberg is the source of truth; GitHub is downstream only.
- Never manually push Ralph Workflow source changes to GitHub unless it is the sync script doing the Codeberg→GitHub mirror update.
- User explicitly requires that before any code push in any repo, I must satisfy that repo's `AGENTS.md` and/or `CLAUDE.md` requirements first.
- Stronger operating rule: if I feel like I need to ask for anything operational, the default answer is "it is up to me" unless the action is genuinely irreversible, externally sensitive, or unsafe to guess.
- User explicitly corrected Reddit voice: Ralph Workflow Reddit comments should not sound like AI or polished marketing copy; they should feel like natural native-speaker Reddit comments, including occasional minor grammatical roughness where it helps authenticity.
- User explicitly requires the self-improvement part of the marketing loop to be genuinely evaluative: it must measure outcomes, identify flat/failing tactics, and replace them instead of confusing activity with progress.
- User explicitly requires the marketing loop to self-reflect and improve without human intervention; if any operational question comes up where help might be requested, the default answer is that it is up to me to decide, act, verify, and continue.
- User clarified the deeper conceptual framing: Ralph Workflow is fundamentally built on Ralph-loop ideas, but upgraded into a composable loop framework.
- The important idea is layered looping: planning can loop, development iteration can loop, and the overall process can loop too, with explicit handoffs between phases.
- This is part of why Ralph Workflow is compelling: a simple concept at the core, but extendable into something strong enough for real software engineering tasks.
- Strong user preference reaffirmed repeatedly: I should never ask for the user's opinion on safe/internal operational choices; the answer is always that it is up to me unless the action is irreversible, externally sensitive, or unsafe to guess.
- Marketing self-improvement must be action-taking, not recommendation-only: when the loop detects a failing tactic and a safe local repair path exists, it should patch/replace the tactic in the same run rather than merely suggesting changes.
- User explicitly set the default model preference to `openai/gpt-5.4`.
- User explicitly instructed me to use OpenCode for all code editing going forward, add skills/plugins as needed, and treat operational questions as mine to decide.
