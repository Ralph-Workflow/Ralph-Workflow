# 2026-05-21

- Durable operating preference clarified further: for codebase-related work, especially complex tasks, OpenCode is an allowed and preferred path to use proactively rather than only after being asked again. This applies beyond Ralph-Site and should influence how I choose implementation paths for substantial repo changes.
- Added a runtime-owned `agent-architecture-watchdog` cron on 2026-05-21 to audit the whole agent system every 6 hours for self-improving-loop quality, loop overlap, ownership drift, stale loop layers, hidden repair layers, and self-certification risks. It writes architecture audit artifacts under `agents/system/logs/` and is expected to repair safe local architecture issues in the same run.
- Centralized the Ralph Workflow docs rubric on 2026-05-21 at `docs/code-style/documentation-rubric.md` and strengthened it with mandatory minimum page-family structure, stronger route/system rules, and a hard requirement that every docs-related change refer to that rubric. Updated both repo-side docs instructions and the runtime docs watchdog to treat that file as the canonical docs-quality standard.
- Corrected Apollo mailbox handling on 2026-05-21: verification codes for Apollo should be checked in `ken@hireaegis.com`, not `ken@ralphworkflow.com`. Updated `TOOLS.md` so future login/recovery work uses the right mailbox by default.
- Repaired the Reddit marketing loop on 2026-05-21 so newer learnings are runtime-enforced instead of living only in `REDDIT_LEARNINGS.md`: `reddit_autopost.py` now prioritizes review-tax / cleanup / bounded-run angles, hard-gates drafts toward site finish-state language, rejects default last-slot product mentions, and adds a one-paragraph draft option; `reddit_next_window_packet.py` now rejects stale cached “fresh bodies” that bypass those gates.
- Architecture audit finding saved on 2026-05-21: several non-docs loops still present as `tracked_only` rather than full checker/runner/verifier contracts, especially the marketing stack. Treat any claimed self-improving loop without explicit remediation plus independent verification evidence as degraded until upgraded or explicitly reclassified as monitor-only.
- Durable instruction reinforced on 2026-05-21: never wait for a follow-up prompt when a safe internal fix path exists; fix it immediately.
- On 2026-05-21 I cleaned accidental duplicate entries out of `memory/2026-05-21.md`, upgraded the marketing loop and agent-architecture watchdog from ambiguous `tracked_only` entries into real full checker/runner/verifier contracts, and explicitly reclassified the remaining non-self-improving loops as `owner_only` or `monitor_only` so the loop-integrity audit no longer hides contract ambiguity.
# 2026-05-21

- Durable operating preference clarified further: for codebase-related work, especially complex tasks, OpenCode is an allowed and preferred path to use proactively rather than only after being asked again. This applies beyond Ralph-Site and should influence how I choose implementation paths for substantial repo changes.
- Added a runtime-owned `agent-architecture-watchdog` cron on 2026-05-21 to audit the whole agent system every 6 hours for self-improving-loop quality, loop overlap, ownership drift, stale loop layers, hidden repair layers, and self-certification risks. It writes architecture audit artifacts under `agents/system/logs/` and is expected to repair safe local architecture issues in the same run.
- Centralized the Ralph Workflow docs rubric on 2026-05-21 at `docs/code-style/documentation-rubric.md` and strengthened it with mandatory minimum page-family structure, stronger route/system rules, and a hard requirement that every docs-related change refer to that rubric. Updated both repo-side docs instructions and the runtime docs watchdog to treat that file as the canonical docs-quality standard.
- Corrected Apollo mailbox handling on 2026-05-21: verification codes for Apollo should be checked in `ken@hireaegis.com`, not `ken@ralphworkflow.com`. Updated `TOOLS.md` so future login/recovery work uses the right mailbox by default.
- Repaired the Reddit marketing loop on 2026-05-21 so newer learnings are runtime-enforced instead of living only in `REDDIT_LEARNINGS.md`: `reddit_autopost.py` now prioritizes review-tax / cleanup / bounded-run angles, hard-gates drafts toward site finish-state language, rejects default last-slot product mentions, and adds a one-paragraph draft option; `reddit_next_window_packet.py` now rejects stale cached “fresh bodies” that bypass those gates.
- Architecture audit finding saved on 2026-05-21: several non-docs loops still present as `tracked_only` rather than full checker/runner/verifier contracts, especially the marketing stack. Treat any claimed self-improving loop without explicit remediation plus independent verification evidence as degraded until upgraded or explicitly reclassified as monitor-only.
- Durable instruction reinforced on 2026-05-21: never wait for a follow-up prompt when a safe internal fix path exists; fix it immediately.
- On 2026-05-21 I cleaned accidental duplicate entries out of `memory/2026-05-21.md`, upgraded the marketing loop and agent-architecture watchdog from ambiguous `tracked_only` entries into real full checker/runner/verifier contracts, and explicitly reclassified the remaining non-self-improving loops as `owner_only` or `monitor_only` so the loop-integrity audit no longer hides contract ambiguity.
- New durable architecture requirement from mistlight on 2026-05-21: competitor analysis, market positioning, Reddit/marketing learnings, and adjacent agent findings must work together as one coordinated system rather than isolated report loops. Use the agent-architecture-audit lens to push the stack toward a shared knowledge base with common findings artifacts, cross-agent-readable memory, and enforced reuse so agents write knowledge once and the rest of the system can consume and act on it consistently.
# 2026-05-21

- Durable operating preference clarified further: for codebase-related work, especially complex tasks, OpenCode is an allowed and preferred path to use proactively rather than only after being asked again. This applies beyond Ralph-Site and should influence how I choose implementation paths for substantial repo changes.
- Added a runtime-owned `agent-architecture-watchdog` cron on 2026-05-21 to audit the whole agent system every 6 hours for self-improving-loop quality, loop overlap, ownership drift, stale loop layers, hidden repair layers, and self-certification risks. It writes architecture audit artifacts under `agents/system/logs/` and is expected to repair safe local architecture issues in the same run.
- Centralized the Ralph Workflow docs rubric on 2026-05-21 at `docs/code-style/documentation-rubric.md` and strengthened it with mandatory minimum page-family structure, stronger route/system rules, and a hard requirement that every docs-related change refer to that rubric. Updated both repo-side docs instructions and the runtime docs watchdog to treat that file as the canonical docs-quality standard.
- Corrected Apollo mailbox handling on 2026-05-21: verification codes for Apollo should be checked in `ken@hireaegis.com`, not `ken@ralphworkflow.com`. Updated `TOOLS.md` so future login/recovery work uses the right mailbox by default.
- Repaired the Reddit marketing loop on 2026-05-21 so newer learnings are runtime-enforced instead of living only in `REDDIT_LEARNINGS.md`: `reddit_autopost.py` now prioritizes review-tax / cleanup / bounded-run angles, hard-gates drafts toward site finish-state language, rejects default last-slot product mentions, and adds a one-paragraph draft option; `reddit_next_window_packet.py` now rejects stale cached “fresh bodies” that bypass those gates.
- Architecture audit finding saved on 2026-05-21: several non-docs loops still present as `tracked_only` rather than full checker/runner/verifier contracts, especially the marketing stack. Treat any claimed self-improving loop without explicit remediation plus independent verification evidence as degraded until upgraded or explicitly reclassified as monitor-only.
- Durable instruction reinforced on 2026-05-21: never wait for a follow-up prompt when a safe internal fix path exists; fix it immediately.
- On 2026-05-21 I cleaned accidental duplicate entries out of `memory/2026-05-21.md`, upgraded the marketing loop and agent-architecture watchdog from ambiguous `tracked_only` entries into real full checker/runner/verifier contracts, and explicitly reclassified the remaining non-self-improving loops as `owner_only` or `monitor_only` so the loop-integrity audit no longer hides contract ambiguity.
# 2026-05-21

- Durable operating preference clarified further: for codebase-related work, especially complex tasks, OpenCode is an allowed and preferred path to use proactively rather than only after being asked again. This applies beyond Ralph-Site and should influence how I choose implementation paths for substantial repo changes.
- Added a runtime-owned `agent-architecture-watchdog` cron on 2026-05-21 to audit the whole agent system every 6 hours for self-improving-loop quality, loop overlap, ownership drift, stale loop layers, hidden repair layers, and self-certification risks. It writes architecture audit artifacts under `agents/system/logs/` and is expected to repair safe local architecture issues in the same run.
- Centralized the Ralph Workflow docs rubric on 2026-05-21 at `docs/code-style/documentation-rubric.md` and strengthened it with mandatory minimum page-family structure, stronger route/system rules, and a hard requirement that every docs-related change refer to that rubric. Updated both repo-side docs instructions and the runtime docs watchdog to treat that file as the canonical docs-quality standard.
- Corrected Apollo mailbox handling on 2026-05-21: verification codes for Apollo should be checked in `ken@hireaegis.com`, not `ken@ralphworkflow.com`. Updated `TOOLS.md` so future login/recovery work uses the right mailbox by default.
- Repaired the Reddit marketing loop on 2026-05-21 so newer learnings are runtime-enforced instead of living only in `REDDIT_LEARNINGS.md`: `reddit_autopost.py` now prioritizes review-tax / cleanup / bounded-run angles, hard-gates drafts toward site finish-state language, rejects default last-slot product mentions, and adds a one-paragraph draft option; `reddit_next_window_packet.py` now rejects stale cached “fresh bodies” that bypass those gates.
- Architecture audit finding saved on 2026-05-21: several non-docs loops still present as `tracked_only` rather than full checker/runner/verifier contracts, especially the marketing stack. Treat any claimed self-improving loop without explicit remediation plus independent verification evidence as degraded until upgraded or explicitly reclassified as monitor-only.
- Durable instruction reinforced on 2026-05-21: never wait for a follow-up prompt when a safe internal fix path exists; fix it immediately.
- On 2026-05-21 I cleaned accidental duplicate entries out of `memory/2026-05-21.md`, upgraded the marketing loop and agent-architecture watchdog from ambiguous `tracked_only` entries into real full checker/runner/verifier contracts, and explicitly reclassified the remaining non-self-improving loops as `owner_only` or `monitor_only` so the loop-integrity audit no longer hides contract ambiguity.
- New durable architecture requirement from mistlight on 2026-05-21: competitor analysis, market positioning, Reddit/marketing learnings, and adjacent agent findings must work together as one coordinated system rather than isolated report loops. Use the agent-architecture-audit lens to push the stack toward a shared knowledge base with common findings artifacts, cross-agent-readable memory, and enforced reuse so agents write knowledge once and the rest of the system can consume and act on it consistently.
- Full loop-process sweep on 2026-05-21: docs loop is independently green, architecture loop is independently green, system-health monitor is green with explicit artifact checks and split latest/history outputs, blocked-channel recovery is owner-only and collapsed to one active owner path, and the marketing loop is the only full-contract loop still independently red for real outcome reasons (Apollo verification block, no recent Reddit post, flat primary-repo adoption). Also note: `agent_architecture_latest.json` can lag behind newer runtime code repairs, so future audits must compare the live loop code/artifacts directly instead of trusting one older architecture report blindly.
