# 2026-05-21

- Docs watchdog runtime root cause identified during pre-compaction flush: `agents/docs_quality/ralph_docs_agentic_review.py` currently turns `RALPH_DOCS_WATCHDOG_SKIP_AGENTIC=1` into a synthetic hard fail (`status=fail`, `loopHealthy=false`, `shouldUserNeedToRepeatThis=true`). This poisons `docs_stack_temp_watchdog.py` health evaluation even when bounded watchdog mode is intentionally skipping the expensive full agentic review.
- Next concrete repair after flush: change watchdog-skip behavior to reuse the last valid full `ralph_agentic_latest.json` verdict if present instead of manufacturing failure; only fail if no valid prior full verdict exists. This preserves bounded watchdog execution without self-poisoning autonomy.
- Also fix `agents/docs_quality/test_docs_stack_temp_watchdog.py` mock signatures so patched `run_py(..., watchdog_mode=..., skip_agentic=...)` style kwargs do not crash tests.
- User explicitly called out that the blocked/skip exception is problematic because the system is expected to be fully autonomous; treat this as a priority runtime semantics fix, not just a reporting issue.
- Stronger standing rule reaffirmed on 2026-05-21: if anything looks blocked or seems to require outside/human action, the default answer is "it is up to me." Do not surface human-intervention requests for operational work; own the problem, keep repairing locally, and treat every blocked item as something I must route around or solve myself unless a true safety boundary applies.
