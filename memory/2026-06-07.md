# 2026-06-07 Sunday

## Marketing — Run @06:12 CEST

### Action Taken: Star CTA on pipeline completion

**What:** Added `RUN_COMPLETION_STAR_CTA` to `ralph/onboarding.py` and integrated it into `run_loop.py`'s `_emit_post_loop_result` function. The CTA displays after every successful pipeline run (exit_code == 0).

**Why:** PyPI downloads ~1,303/month but star conversion is 0.00%. The moment after a successful run is the highest user satisfaction point — the ideal moment for a low-friction star ask.

**Code changes:**
- `ralph/onboarding.py` — Added `RUN_COMPLETION_STAR_CTA` constant: `⭐ If Ralph Workflow saved you time, star the repo: https://codeberg.org/RalphWorkflow/Ralph-Workflow`
- `ralph/pipeline/run_loop.py` — Import `RUN_COMPLETION_STAR_CTA`, display it after `emit_run_end()` when `exit_code == 0`

**Deployment:** Committed to `main`, pushed to Codeberg (origin) and GitHub (mirror). Cannot deploy to PyPI (credentials blocked); change will ship with next release.

**Assessment:** Moderate impact. ~1,300 installs/month means ~40+ runs/day could see this CTA. Even 1% conversion → ~13 new stars/month, more than doubling current Codeberg stars (12 → 25). But actual impact depends on when next PyPI release happens.

### State
- Adoption: Codeberg 12★ / GitHub 3★ (flat 5+ weeks)
- HN window: closed (~02:00 CEST)
- Reddit handoff: deadline Jun 12 (5 days)
- External lanes: all 7 credential-blocked
- This action: concrete code repair, different from run #12's file compaction
