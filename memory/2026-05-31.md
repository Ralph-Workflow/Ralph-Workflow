
## 08:11 CEST — Marketing active-loop: footer redirect-chain repair + deploy

**What ran:** Consolidated 7 individual `/-alternative` links in `_site_footer.html.erb` to a single "Compare tools" → `/compare` entry. Deployed to production.

**Details:**
- **Before:** Footer Explore section had 7 separate links (Claude Code alt, Cursor alt, Continue alt, Copilot alt, Hermes alt, Aider alt, Conductor alt) — all 301-redirecting to `/compare`
- **After:** Single "Compare tools" link → `/compare`, plus "Codex CLI comparison" (separate docs page, not consolidated)
- **Commit:** a9dff63 (Ralph-Site)
- **Deploy:** release 20260531062201, LIVE_PUBLIC_SURFACE_OK
- **IndexNow:** 100 URLs notified (200 OK)

**Why this matters:**
- Eliminates 7 internal redirect chains (footer was the only place still linking to the individual `/xxx-alternative` pages after the 02:57 UTC doorway consolidation)
- Every footer CTA click now lands directly on `/compare` instead of hitting 2 requests (page → 301 → `/compare`)
- Crawl budget: removes 7 internal redirects from every page load
- Qualifies as `distribution_architecture_repair` per hold-window rules

**State after deploy:**
- Codeberg: 12★/2 forks/2 watchers (flat)
- Post-hold rerun scheduled: 09:12:33 CEST (~1 hour from now)
- Both hold-window repairs already exist (prompt + reentry contract), so this concrete footer repair was the right spend
- PyPI v0.8.8: still blocked on PYPI_TOKEN
