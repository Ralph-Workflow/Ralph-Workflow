# 2026-05-23

- Fixed a real health-monitor escalation design bug: repeat issues were being tagged as `escalation_required` only after the repair phase, so stronger owner-loop escalation could not trigger in the same run.
- Added docs-loop stability tracking as a first-class health signal. The docs verifier now has machine-readable status/history artifacts (`ralph_verifier_latest.json`, `ralph_verifier_history.jsonl`) and the system treats repeat docs failures as an open instability window until clean passes are re-earned.
- Health monitor now tracks repeat issues across recent history instead of trusting only the latest artifact, and architecture verification can fail closed on unstable docs-loop history.
- Confirmed the docs problem had shifted from content gaps to trust/stability gaps: one lingering configuration-page positioning drift and insufficient clean-pass recovery after repeat failures. The remaining top-level red state after docs cleanup was marketing/architecture, not docs content.
