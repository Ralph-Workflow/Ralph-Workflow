# Ralph Workflow Demo Captures — archived historical output

These captures are **archived historical output** from Ralph Workflow v0.8.8
(older release; output shape, agent roster, and command surface may differ
in current releases). The current proof of what a successful run leaves you
is the inline finish-receipt block in [`../../README.md`](../../README.md)
under "What a run leaves you" — that block is hand-written and tracks the
shipped runtime.

## Files

| File | Description |
|------|-------------|
| `init-output.txt` | `ralph --init` on a fresh project (v0.8.8) |
| `diagnose-output.txt` | `ralph --diagnose` — agent + config validation (v0.8.8) |
| `dry-run-output.txt` | `ralph --dry-run` — full pipeline preview (v0.8.8) |

## Watch the Demo

The asciinema recording was removed (hosting expired).

[📺 Watch the first-run walkthrough →](https://ralphworkflow.com/blog/ralph-workflow-in-5-minutes)
*These are genuine, unedited outputs from v0.8.8 — no mock-ups.*
