# phases/planning.toml — Normative planning phase definition
#
# Planning produces the work plan for the current development cycle.
# It may produce either a sequential plan (single agent) or a parallel
# plan (multiple work_units[]) which the orchestrator dispatches to
# concurrent development agents.

phase_id = "planning"
origin = "shipped_default"
drain = "planning"
template_key = "planning"
artifact_profile = "planning"
next = ["development"]
