Task: Produce and submit one spec-compliant MCP commit_message artifact for the changes not yet committed—the current worktree vs the last commit.

{% from 'shared/_artifact_submission.j2' import render_artifact_submission %}
{% from 'shared/_optional_artifact_skill_pointer.j2' import render_optional_artifact_skill_pointer %}
{% from 'shared/_payload_section.j2' import render_payload_section %}

{% include 'shared/_unattended_mode.j2' %}

{{ render_payload_section('DIFF', DIFF, DIFF_PATH) }}

Read the diff, choose a commit or skip document, and submit it with the tool named below. Do not call bash or ask the user for confirmation, including “Would you like me to…?”.

## HOW TO WRITE THE SUBJECT

1. Choose the first matching kind:

| The diff mainly... | kind |
|---|---|
| changes only repo maintenance, tooling, config, or dependencies | chore |
| changes only CI/workflow files | ci |
| changes only build scripts or packaging | build |
| changes only documentation or comments | docs |
| changes only tests | test |
| changes only formatting, with no behavior change | style |
| fixes wrong behavior | fix |
| adds behavior that did not exist | feat |
| restructures code with no behavior change | refactor |
| changes code to make it faster | perf |

2. Use the directory or subsystem that owns most of the diff as the scope. Otherwise omit the scope and the parentheses entirely.
3. Write the first valid lowercase imperative subject; do not generate alternatives. There is no length limit.
4. Confirm `<kind>(<scope>)?!?: <description>`: a scope uses lowercase letters, digits, `/`, `_`, or `-` only — no dots, no uppercase; `!` marks a breaking change; one space follows the colon; the description starts with a lowercase letter or digit; and the frontmatter value is unquoted. Quotes become part of the subject and are rejected.

## HOW TO WRITE THE BODY

For any change beyond a one-line typo, formatting, or comment change, include one `## Body` item explaining why it was needed and what it affects. Use `## Body Summary`, `## Body Details`, and `## Body Footer` for migration steps, breaking changes, or issue references.

## Document shape

Choose a commit document for pending work or a skip document when no commit is needed.

- Commit document: frontmatter `type: commit` and `subject:`. Include `## Body` (one concise self-contained item) or the detailed `## Body Summary` / `## Body Details` / `## Body Footer` set, plus optional `## Files` and `## Excluded Files` (`path | reason`, where valid reasons are internal_ignore, not_task_related, sensitive, deferred).

```markdown
---
type: commit
subject: fix(auth): prevent token expiry race
---

## Body Summary
- [BS-1] Prevents a race where refreshed tokens could expire mid-request.

## Body Details
- [BD-1] The expiry check now happens after refresh; covers the concurrent-refresh edge case.

## Excluded Files
- [X-1] notes/todo.md | not_task_related
```

- Skip document when no commit is needed:

```markdown
---
type: skip
reason: Only generated artifacts changed; nothing to commit.
---
```

{% set commit_message_example %}
---
type: commit
subject: fix(auth): prevent token expiry race
---

## Body
- [B-1] Explains the expiry race and why the check moved after refresh.
{% endset %}
{{ render_artifact_submission('commit_message', SUBMIT_MD_ARTIFACT_TOOL_REFERENCE, commit_message_example, write_file_tool_reference=WRITE_FILE_TOOL_REFERENCE) }}

{{ render_optional_artifact_skill_pointer('submit-commit-message-artifact', '.agent/artifact-formats/commit_message.md') }}
