Task: produce a spec-compliant MCP commit_message artifact for the diff below.
Read the diff silently. Do not write an analysis.

{% from 'shared/_payload_section.j2' import render_payload_section %}

{% include 'shared/_unattended_mode.j2' %}

---
## COMMIT MESSAGE FORMAT
<type>[optional scope][!]: subject

Use the types feat/fix/docs/refactor/test/style/perf/build/ci/chore. Use `chore` only for repo maintenance, tooling, or admin-only changes; do not default to `chore` for meaningful product or code changes. Keep the subject <= 50 chars and write it in lowercase imperative mood.

## WHEN TO USE A BODY

Most commits need a body. A body provides context, rationale, and impact that the subject alone cannot convey.

**Include a body when the change is non-trivial:**
- Multi-file changes or changes affecting multiple subsystems
- New behavior, removed behavior, or behavior modifications
- Refactors that change public APIs, data structures, or contracts
- Changes requiring migration notes, deprecation warnings, or compatibility concerns
- Any change where the *why* is not immediately obvious from reading the diff

**One-liner subjects (no body) are only acceptable for:**
- Trivial typo fixes or single-character corrections
- Single-file cosmetic changes (formatting, imports, comments) with obvious intent
- Truly obvious micro-changes where subject fully captures the entire change

## BODY STRUCTURE: WHEN TO USE SIMPLE VS DETAILED

For commits requiring a body, choose the right structure:

**Use `body` (single string) when:**
- The explanation is concise and self-contained (1-2 paragraphs)
- The rationale is straightforward and doesn't need separation

**Use detailed body fields when the change is complex:**
- `body_summary`: What changed and why in 1-2 sentences
- `body_details`: Deep dive — edge cases handled, design decisions, alternatives considered, behavioral notes
- `body_footer`: Breaking changes, migration steps, deprecation warnings, "Fixes #123", "Closes #456"

Complex refactors, architecture changes, new features with non-obvious behavior, and any change affecting multiple subsystems should use the detailed structure.

## QUALITY RULES
- Match the repo's conventional-commit style. Prefer the narrowest stable scope when one subsystem clearly owns most of the diff.
- Omit the scope when the change spans multiple subsystems or no single scope would be honest.
- The subject must say what changed, not that files changed. Avoid filler like "update", "misc", "stuff", or file-count summaries.
- Body text should explain impact and rationale, not restate which files changed or re-summarize the diff.
- When in doubt, include a body. An extra explanatory paragraph is almost never wrong.

## COMMON MISTAKES TO AVOID
- Bad: chore: update files
  Counterexample: feat(mcp): add structured commit retries
  Body: Explains what structured retries solve and how they work.
- Bad: fix: stuff
  Counterexample: fix(parser): preserve prefixed transcript lines
  Body: Explains why prefixed lines were being dropped and the edge case that triggered it.
- Bad: refactor: rename files
  Counterexample: refactor(auth): extract token validation to dedicated module
  Body: Explains the architectural reason and what callers must do differently.
- Bad: feat: add validation
  Counterexample: feat(api): add query filter validation for export endpoint
  Body: Explains which invalid inputs were causing problems and what happens now.

## OUTPUT RULES
After submitting the MCP artifact, you may echo only the commit subject line as plain text.
Do not output any additional explanation, markdown formatting, bullets, or prose after the subject line.
Do not wrap JSON or the subject in code fences.
Do not ask the user for confirmation or write "Would you like me to..." questions.
Submit the artifact now, not as a proposal for later approval.
Call the tool before emitting any final text. If you skip the tool call, the task has failed.
Do not call Bash. Do not use `content_path` for this task.

MCP REQUIREMENT:
The only tool you may call for this task is {{ SUBMIT_ARTIFACT_TOOL_INSTRUCTIONS }}.
You MUST submit the final result by calling it with `artifact_type="commit_message"` and `content` set to a JSON string matching one of these shapes:
{"type": "commit", "subject": "type(scope): description"}
{"type": "commit", "subject": "type(scope): description", "body": "Optional multi-line body"}
{"type": "commit", "subject": "type(scope): description", "body_summary": "Brief summary", "body_details": "Detailed explanation", "body_footer": "Fixes #123"}
{"type": "commit", "subject": "type(scope): description", "files": ["src/auth/token.rs", "tests/auth/token_expiry_test.rs"]}
{"type": "commit", "subject": "type(scope): description", "excluded_files": [{"path": "notes/todo.md", "reason": "not_task_related"}]}
{"type": "skip", "reason": "Reason why no commit is needed"}.

Submit the MCP arguments in this shape:
{"artifact_type":"commit_message","content":"{\"type\":\"commit\",\"subject\":\"type(scope): description\"}"}

Schema rules: use exactly one of commit/skip, use EITHER body OR the detailed body fields, never both, and only include files/excluded_files when you need selective commit scope. `files` must be an array of strings. `excluded_files` must be an array of objects with exactly `path` and `reason` fields. Valid `excluded_files.reason` values: internal_ignore, not_task_related, sensitive, deferred.

REQUIRED PROCEDURE:
1. Read the diff and decide whether the correct result is a commit artifact or a skip artifact.
2. Build the JSON content with the exact schema above.
3. Immediately call the tool with `artifact_type="commit_message"` and that JSON string as the `content` argument.
4. After the MCP call succeeds, optionally echo only the commit subject line once and nothing else. If you submitted a skip artifact, do not emit extra prose.
5. If the submit-artifact MCP tool is unavailable in this turn, use `write_file` to write the raw commit payload JSON to `.agent/tmp/commit_message.json` instead. Write ONLY the inner payload object such as `{"type":"commit","subject":"fix(scope): message"}` or `{"type":"skip","reason":"..."}`. Do not wrap it in artifact metadata.

{{ render_payload_section('DIFF', DIFF, DIFF_PATH) }}

After the MCP submission, you may optionally echo the same single-line subject as plain text,
but the MCP artifact is the authoritative output. Do not emit markdown, bullets, or explanations
as the final answer.
