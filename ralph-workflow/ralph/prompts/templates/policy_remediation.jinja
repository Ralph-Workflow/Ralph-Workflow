Author and verify policy documents under {{ canonical_dir }} so the readiness validator accepts verified project reality.

{% include 'shared/_unattended_mode.j2' %}

Resolve every finding from current repository evidence. Keep each policy a living document: preserve stricter established rules, resolve conflicts in favor of the existing project policy without weakening policy intent, keep facts machine-checkable where required, and record applicable unresolved work as a dated deferral with a concrete review trigger.

## Findings

{{ findings_block }}
{% if analysis_feedback_block %}

## Accepted work and remaining feedback

Keep accepted policy intact and resolve the remaining findings.

> {{ analysis_feedback_summary }}

{{ analysis_feedback_block }}
{% endif %}

## Work from evidence

Inventory languages, manifests, contributor guidance, tools, and the authoritative verification entry point before declaring facts or commands. Use read-only subagents for independent policy or gate investigations when helpful; reconcile terminology and edit in the main session.

When adding gate plumbing, follow `{{ gate_script_policy_path }}`: use strict, fail-closed scripts with installed tools and real paths; test their passing and failing behavior; wire them into the authoritative verification entry point; protect secrets and keep remote execution safe. Attribute a product-code failure from a working gate accurately in the policy instead of changing unrelated product code.

## Machine marker vocabulary

The readiness validator consumes these byte-exact forms:

- `RALPH-FACT: <key>: <value>` records a verified project fact.
- `RALPH-FACT: <key>: RALPH-PENDING (assumed <date>); review trigger: <cond>` records a deferred fact.
- `RALPH-COMMAND: <approved-tool …>` records a runnable gate.
- `RALPH-INAPPLICABLE: <reason>` records a gate that never applies.
- `RALPH-PENDING: <approved-tool …> (assumed <date>); review trigger: <cond>` records an applicable deferred gate.
- `RALPH-REVIEW: <procedure>; evidence: <record>; owner: <role>` records required human review.

```text
RALPH-PENDING: pytest (assumed 2026-07-12); review trigger: once test deps are installed
RALPH-FACT: secret_scan_command: RALPH-PENDING (assumed 2026-07-12); review trigger: once a scanner is chosen
```

Every `RALPH-COMMAND` begins with an approved first token: {{ approved_tools }}. Use `RALPH-PENDING` for an applicable unresolved requirement and `RALPH-INAPPLICABLE` only when it never applies. Each applicable typecheck or lint language has `RALPH-LANG: <Language>` followed by its command, inapplicable declaration, or deferral.

## Complete the policy corpus

INSPECT the project before writing. Replace every `RALPH-FACT:` line and placeholder with verified policy; remove `RALPH-STARTER-TEMPLATE` and `REPLACE-ME` instructions after applying them. There is no completion marker: completion is the absence of unresolved markers. Add a `## Research basis` citation block and record each inactive conditional domain with its dated reactivation trigger. Move policy content into its canonical file and retain `{{ migrated_marker }}` at conventional locations. Probe declared gates, distinguish broken plumbing from product-code failures, and record the attribution. Keep exactly one concise managed block between `{{ agents_block_begin }}` and `{{ agents_block_end }}` in AGENTS.md; if CLAUDE.md exists, point it to AGENTS.md.

## Finish with a truthful report

Report changed files, migrations, research sources, probes, and unresolved risks. The validator re-runs after your response: completion means every finding is resolved, required markers are well formed, and no unresolved placeholder remains.
