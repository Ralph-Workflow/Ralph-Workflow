CRITICAL: Never invoke Git through the exec tool or any runtime-native shell.
Inspect repository state only through the brokered `git_status`, `git_diff`,
`git_log`, and `git_show` tools that are present in this session. If a brokered
Git read tool is absent, do not substitute shell Git. Ralph Workflow alone may
stage, commit, or push changes.
