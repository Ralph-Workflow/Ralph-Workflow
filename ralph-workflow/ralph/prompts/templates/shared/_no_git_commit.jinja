CRITICAL: Do NOT run ANY git command except read-only lookup commands (e.g. `git status`, `git diff`, `git log`, `git show`). Ralph Workflow is the only actor that may commit, push, or run git staging commands (git add).
