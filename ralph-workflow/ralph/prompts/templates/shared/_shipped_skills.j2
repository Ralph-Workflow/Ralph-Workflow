{% if SKILLS_INLINE_CONTENT %}
## SHIPPED SKILL BUNDLE

{{ SKILLS_INLINE_CONTENT }}
{% else %}
## SHIPPED SKILLS

Ralph Workflow installs skills where your agent discovers them automatically. Your agent runtime exposes installed skills from its skill directories at session start — Ralph Workflow does not list skill names here. Use whatever skills your environment already makes available.

Invoke relevant available skills through your agent's skill mechanism before proceeding. Do not Read or browse skill files from disk yourself.
{% if shipped_skills_mode|default('') == 'development' %}

When the execution plan includes a Skills and MCPs section, invoke those skills through your agent's skill mechanism before proceeding.
{% endif %}
{% endif %}
{% if shipped_skills_mode|default('') == 'planning' %}

Before authoring the plan's `## Skills MCP` section, choose task-relevant skills from those already available to you, then record their names on its `Skills:` line.
{% endif %}
