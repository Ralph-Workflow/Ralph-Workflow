{# Single OPTIONAL skill pointer shared across the planning-family and
   artifact-producing templates. Import via:

     {% from 'shared/_optional_artifact_skill_pointer.j2' import render_optional_artifact_skill_pointer %}

   Then call:

     {{ render_optional_artifact_skill_pointer(skill_name, format_doc) }}

   Renders a short OPTIONAL-skill block pointing at the named skill while
   keeping the format doc as the source of truth.
#}
{% macro render_optional_artifact_skill_pointer(skill_name, format_doc) -%}
## OPTIONAL: {{ skill_name }} skill

The `{{ skill_name }}` skill is OPTIONAL — invoke it by name through your agent's skill mechanism. You may consult it for a worked example and a recovery path for rejected documents. The canonical document grammar and section shapes remain in `{{ format_doc }}` — that is the source of truth.
{%- endmacro %}
