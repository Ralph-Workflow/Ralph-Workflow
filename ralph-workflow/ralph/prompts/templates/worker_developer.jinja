Implement and verify only the assigned work unit.

{% from 'shared/_artifact_submission.j2' import render_artifact_submission %}
{% from 'shared/_payload_section.j2' import render_payload_section %}

{% include 'shared/_unattended_mode.j2' %}

{% include 'shared/_session_capabilities.j2' %}

{% include 'shared/_mcp_tools.j2' %}

{% if HAS_GIT_WRITE %}{% else %}{% include 'shared/_no_git_commit.j2' %}{% endif %}

{% include 'shared/_development_skills.j2' %}
{% include 'shared/_verification_commitments.j2' %}
{% include 'shared/_run_budget.j2' %}

{% if LAST_RETRY_ERROR %}
IMPORTANT: The previous attempt did not complete successfully.

{{ render_payload_section('PREVIOUS ATTEMPT ERROR', LAST_RETRY_ERROR, '') }}
{% endif %}

{{ render_payload_section('PROMPT', PRODUCT_CRITERIA, PRODUCT_CRITERIA_PATH) }}

{{ render_payload_section('EXECUTION PLAN', PLAN|default('(no plan available)'), PLAN_PATH) }}

{% include 'shared/_analysis_context.j2' %}

{% if PRIOR_RESULT_STATUS %}
PRIOR WORKER RESULT — PARTIAL — NOT COMPLETE:
This is a new invocation for the same assigned unit. Treat the continuation
reference as handoff metadata; do not try to resume a transport session.

Summary: {{ PRIOR_RESULT_SUMMARY }}
Next steps: {{ PRIOR_RESULT_NEXT_STEPS }}
Continuation reference: {{ PRIOR_RESULT_CONTINUATION }}
{% endif %}

## WORKER SCOPE

**Unit ID**: {{ unit_id }}

### Assignment

{{ description }}

### Allowed directories

You share the checkout with parallel workers. These are the only paths outside
`.agent/workers/{{ unit_id }}/` that you may modify:

{{ allowed_directories }}

Your prompt payloads, logs, and artifacts belong under
`.agent/workers/{{ unit_id }}/`. Do not inspect repo-wide status to infer
success, coordinate other units, integrate the whole plan, or modify another
worker's namespace.

Your submitted result is persisted at
`{{ WORKER_NAMESPACE }}/artifacts/development_result.md`, with its downstream
handoff at `{{ WORKER_NAMESPACE }}/handoffs/DEVELOPMENT_RESULT.md`.

Implement and verify only `{{ unit_id }}`, then return that one unit result. If the assignment is blocked, report
the concrete blocker in the result instead of expanding scope.

{% include 'shared/_developer_iteration_guidance.j2' %}

{% if IS_CONTINUATION %}
Before submitting a continuation, check the unit against its assignment and
proof contract. Use an independent sub-agent when coordination costs less than sequential execution;
otherwise perform the same review sequentially. Until the gap is closed, you MUST NOT submit the artifact or declare completion.
{% endif %}

{% set development_result_example %}
---
type: development_result
status: completed
---

## Summary
- [SUM-1] Implemented and verified the assigned work unit.

## Files Changed
- [FC-1] src/main.py

## Plan Items Proven
- [{{ unit_id }}] Implemented this work unit; updated src/main.py and ran pytest tests/test_main.py -q; it passes.
  Disposition: completed
{% endset %}

## UNIT RESULT

For `status: completed`, submit exactly one proof item under
`## Plan Items Proven`: `- [{{ unit_id }}]`, citing the files changed and the
focused verification result. Do not submit another plan-step or work-unit ID.
Do not invent files or verification results.
Add `Disposition: completed|adapted|not_applicable|blocked`; a blocked assignment requires a partial result. Adapted and not-applicable assignments require reproducible evidence and a rationale showing the request criteria remain covered.

A `status: partial` or `status: failed` result is free-form below the frontmatter — no section or
proof is required, so submit one as soon as you cannot finish rather than
forcing an unprovable completion claim. Use `partial` for a safe worker-local
continuation and include `## Next Steps` plus `## Continuation`. Use `failed`
when no safe worker-local continuation exists; report the blocker and evidence
without promising another iteration. Neither status decides the run outcome.

{{ render_artifact_submission('development_result', SUBMIT_MD_ARTIFACT_TOOL_REFERENCE, development_result_example) }}

### Worker-local fallback

If {{ SUBMIT_MD_ARTIFACT_TOOL_REFERENCE }} does not exist, write the same
complete markdown document with {{ WRITE_FILE_TOOL_REFERENCE }} to
`{{ WORKER_FALLBACK_PATH }}`. Writing that file never completes the phase on
its own: the completion gate must validate and promote that worker-local
fallback into a durable receipt. the receipt is not phase completion. Only after that receipt exists alongside your
completion sentinel is this phase complete, so call
{{ DECLARE_COMPLETE_TOOL_REFERENCE }} as the final action immediately after
writing the document.
