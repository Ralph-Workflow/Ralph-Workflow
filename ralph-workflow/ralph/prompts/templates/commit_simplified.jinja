{% from 'shared/_artifact_submission.j2' import render_artifact_submission %}
{% from 'shared/_payload_section.j2' import render_payload_section %}

The diff below contains only the current pending work — changes not yet committed (current worktree vs the last commit, HEAD). Do not analyze anything. Read the diff silently. The only tool you may call is `{{ SUBMIT_ARTIFACT_TOOL_NAME }}`. Do not call bash. Do not call any other tool. Do not use `content_path` for this task. Do not ask the user for confirmation. Do not write 'Would you like me to' or equivalent questions. Match the repo's conventional-commit style. Use `chore` only for repo maintenance. Omit the scope when the change spans multiple subsystems. Most commits need a body. Include a body when the change is non-trivial: multi-file changes, behavior changes, refactors affecting APIs or data structures, migration notes, or when the *why* is not obvious from the diff. One-liner subjects (no body) are only for trivial changes like typo fixes or single-file cosmetic changes. For complex changes requiring a body, use the detailed structure: body_summary (what and why), body_details (edge cases, design decisions, alternatives), body_footer (migration, breaking changes, Fixes #123). Avoid vague subjects: Bad "chore: update files" -> Good "feat(mcp): add structured commit retries" with body. Bad "fix: stuff" -> Good "fix(parser): preserve prefixed transcript lines" with body. Immediately call `{{ SUBMIT_ARTIFACT_TOOL_NAME }}` with `artifact_type="commit_message"` and `content` containing a JSON string. Use this MCP argument shape: {"artifact_type":"commit_message","content":"{\"type\":\"commit\",\"subject\":\"type(scope): description\"}"}. Optional commit fields are "body", or the detailed trio "body_summary"/"body_details"/"body_footer", plus optional "files" and "excluded_files". Use the JSON string {"type":"commit","subject":"type(scope): description","excluded_files":[{"path":"notes/todo.md","reason":"not_task_related"}]} when you need excluded files. `files` must be an array of strings. `excluded_files` must be an array of objects with exactly `path` and `reason` fields. Valid `excluded_files.reason` values: internal_ignore, not_task_related, sensitive, deferred. If no commit should be created, use the JSON string content {"type":"skip","reason":"Reason why no commit is needed"}. Do not use the legacy {"message": ...} format.

{{ render_artifact_submission('commit_message', SUBMIT_ARTIFACT_TOOL_REFERENCE) }}

REQUIRED PROCEDURE:
1. Read the diff and decide whether the correct result is a commit artifact or a skip artifact.
2. Build the JSON content with the exact schema above.
3. Immediately call the tool with `artifact_type="commit_message"` and the JSON string as the `content` argument.
4. After the tool call succeeds, if you submitted a commit artifact output only the commit subject line; if you submitted a skip artifact output nothing.
5. If the submit-artifact MCP tool is unavailable in this turn, use `write_file` to write the raw commit payload JSON to `.agent/tmp/commit_message.json` instead. Write ONLY the inner payload object such as {"type":"commit","subject":"fix(scope): message"} or {"type":"skip","reason":"..."}. Do not wrap it in artifact metadata.

{% include 'shared/_unattended_mode.j2' %}

{{ render_payload_section('DIFF', DIFF, DIFF_PATH) }}
