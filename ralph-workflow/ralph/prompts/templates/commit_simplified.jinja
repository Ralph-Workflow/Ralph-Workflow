{% from 'shared/_artifact_submission.j2' import render_artifact_submission %}
{% from 'shared/_payload_section.j2' import render_payload_section %}

The diff below contains only the current pending work — changes not yet committed (current worktree vs the last commit, HEAD). Do not analyze anything. Read the diff silently. The only state-changing tools you may call are `{{ SUBMIT_ARTIFACT_TOOL_NAME }}`, {{ DECLARE_COMPLETE_TOOL_REFERENCE }}, and {{ WRITE_FILE_TOOL_REFERENCE }} only when the shared fallback procedure explicitly permits it. Do not call bash. Do not call any other state-changing tool. Read-only tools are allowed when the shared artifact procedure tells you to inspect the format doc. Do not use `content_path` for this task. Do not ask the user for confirmation. Do not write 'Would you like me to' or equivalent questions. Match the repo's conventional-commit style. Use `chore` only for repo maintenance. Omit the scope when the change spans multiple subsystems. Most commits need a body. Include a body when the change is non-trivial: multi-file changes, behavior changes, refactors affecting APIs or data structures, migration notes, or when the *why* is not obvious from the diff. One-liner subjects (no body) are only for trivial changes like typo fixes or single-file cosmetic changes. For complex changes requiring a body, use the detailed structure: body_summary (what and why), body_details (edge cases, design decisions, alternatives), body_footer (migration, breaking changes, Fixes #123). Avoid vague subjects: Bad "chore: update files" -> Good "feat(mcp): add structured commit retries" with body. Bad "fix: stuff" -> Good "fix(parser): preserve prefixed transcript lines" with body. Submit the result through the documented artifact path: call `{{ SUBMIT_ARTIFACT_TOOL_NAME }}` when it is available, or use the shared write-file fallback only when that tool is unavailable and the prompt's fallback procedure permits it. Use this MCP argument shape: {"artifact_type":"commit_message","content":"{\"type\":\"commit\",\"subject\":\"fix(auth): prevent token expiry race\"}"}. Optional commit fields are "body", or the detailed trio "body_summary"/"body_details"/"body_footer", plus optional "files" and "excluded_files". Use {"type":"commit","subject":"fix(auth): prevent token expiry race","excluded_files":[{"path":"notes/todo.md","reason":"not_task_related"}]} as the content payload when you need excluded files. Use `{"type":"skip","reason":"Reason why no commit is needed"}` when the correct result is a skip artifact. `files` must be an array of strings. `excluded_files` must be an array of objects with exactly `path` and `reason` fields. Valid `excluded_files.reason` values: internal_ignore, not_task_related, sensitive, deferred.

{{ render_artifact_submission('commit_message', SUBMIT_ARTIFACT_TOOL_REFERENCE, '{"type":"commit","subject":"fix(auth): prevent token expiry race"}', write_file_tool_reference=WRITE_FILE_TOOL_REFERENCE) }}

Read the diff and decide whether the correct result is a commit artifact or a skip artifact. Then follow the artifact submission procedure above — it is the SINGLE source of truth for steps 1-5 and the completion contract. The artifact submission procedure above is authoritative for the completion contract; this template does NOT duplicate it.

{% include 'shared/_unattended_mode.j2' %}

{{ render_payload_section('DIFF', DIFF, DIFF_PATH) }}
