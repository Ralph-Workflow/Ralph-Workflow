You are continuing a DEVELOPMENT iteration. Resume from the prior work, follow the current plan, and finish the remaining implementation.

{% from 'shared/_payload_section.j2' import render_payload_section %}

{% include 'shared/_unattended_mode.j2' %}

ORIGINAL REQUEST:
{{ render_payload_section('PROMPT', PROMPT, PROMPT_PATH) }}

IMPLEMENTATION PLAN:
{{ render_payload_section('IMPLEMENTATION PLAN', PLAN|default('(no plan available)'), PLAN_PATH) }}

ANALYSIS FEEDBACK:
{{ render_payload_section('ANALYSIS FEEDBACK', ANALYSIS_FEEDBACK|default(''), ANALYSIS_FEEDBACK_PATH) }}

If you need to revise a saved result artifact during this continuation, edit `.agent/artifacts/development_result.json` and resubmit with `content_path` using this exact call:
`{{SUBMIT_ARTIFACT_TOOL_NAME}}({"artifact_type":"development_result","content_path":".agent/artifacts/development_result.json"})`
{{SUBMIT_ARTIFACT_BARE_HINT}}
