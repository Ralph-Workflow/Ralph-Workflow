# ============================================================================
# LINT POLICY FILE -- DO NOT WEAKEN, DISABLE, OR RELAX RULES WITHOUT EXPLICIT
# USER/MAINTAINER DIRECTION. REFRACTOR THE CODE INSTEAD. "BEING LAZY TO
# REFACTOR" IS NOT A VALID REASON TO CHANGE LINTING BEHAVIOR. THE ONLY
# EXCEPTION IS DOCUMENTED AUTOGENERATED CODE HANDLED BY THE REPOSITORY'S
# APPROVED MECHANISM.
# ============================================================================
# Package-local clippy configuration for ralph-workflow.
# Keep lint policy strict while configuring thresholds for structural lints.

too-many-lines-threshold = 500
max-struct-bools = 32
# ~652KB stack frame detected in test code (clippy emits no source location
# for large_stack_frames/large_stack_arrays when the function is generated
# by macro expansion - known clippy limitation).
# Threshold raised incrementally as test suites grow (more TestDescAndFn entries).
# Last raised after adding XSD tolerant parsing tests for dev-result, fix-result,
# issues, and commit validators which pushed the generated lib-test harness
# from 676584 to 681512 bytes.
stack-size-threshold = 685000
array-size-threshold = 65536
