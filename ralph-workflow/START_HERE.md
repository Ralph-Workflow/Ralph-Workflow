# Start Here: first run as a package operator

This page is the package-side first-run pointer for a reader who has
just installed `ralph-workflow` and is exploring the package
directory. The guided first-run walkthrough lives once on the repo-root
[`START_HERE.md`](../START_HERE.md) — see the **Exact first steps**
section for the six-step path. Use this package-side page only to
discover where the operator manual is rooted and which sphinx pages
cover the per-step detail.
