[pytest]
minversion = 8.0
testpaths = tests
asyncio_mode = auto
markers =
    timeout_seconds(value): override the per-test timeout for slower integration cases
filterwarnings =
    error
    ignore::DeprecationWarning
