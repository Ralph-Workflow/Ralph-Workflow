{# ============================================================================ #}
{# Template: developer_iteration_xsd_retry_continuation.txt                      #}
{# Version: 2.0 - MCP JSON resubmission only                                    #}
{# ============================================================================ #}
{#                                                                             #}
{# PURPOSE:                                                                    #}
{#   Retry prompt when continuation XML validation fails during development.      #}
{#   This is STRICTLY for fixing malformed XML - NO other actions allowed.     #}
{#                                                                             #}
{# VARIABLES:                                                                  #}
{#   {{XSD_ERROR}} - XSD validation error message (required)                   #}
{#                                                                             #}
{# FILES TO READ (REFERENCE ONLY):                                              #}
{#   .agent/tmp/development_result.xsd - The XSD schema (for structure)       #}
{#   .agent/tmp/last_output.xml        - Your previous invalid output (for format) #}
{#                                                                             #}
{# OUTPUT FORMAT:                                                               #}
{#   Valid <ralph-development-result> XML conforming to the XSD schema         #}
{#                                                                             #}
{# IMPORTANT: All three status values (completed/partial/failed) are valid.    #}
{# Do NOT omit completed. FR#7 contract requires all three.                   #}
{{> shared/_unattended_mode}}

{{> shared/_session_capabilities}}

{% if HAS_GIT_WRITE %}{% else %}{{> shared/_no_git_commit}}{% endif %}

═══════════════════════════════════════════════════════════════════════════════
CONTINUATION VALIDATION FAILED
═══════════════════════════════════════════════════════════════════════════════

Your previous continuation submission failed validation.

**THIS IS A SUBMISSION-FIX-ONLY RETRY**

ERROR: {{XSD_ERROR}}

The validation error is in JSON directive format:
{"code": "INVALID_VALUE", "field_path": "status", "next_actions": ["set status to partial or failed"]}

You MUST call `ralph_submit_artifact` with artifact_type="development_result"
and corrected JSON content. Follow every instruction in the `next_actions`
array exactly.

The continuation JSON must include:
{
  "status": "partial",
  "summary": "What was completed and what remains",
  "continuation": {
    "prior_session_id": "...",
    "remaining_steps": ["ordered", "actionable", "recovery", "checklist"]
  }
}

Keep the output focused on why the full plan was not completed.
Include an ordered, actionable recovery checklist that finishes the remaining plan.

Do not modify repository files or do new implementation work in this retry.
Resubmit your corrected JSON via `ralph_submit_artifact`.
