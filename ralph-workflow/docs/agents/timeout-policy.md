# Timeout policy (moved)

This material has been consolidated into the canonical watchdog and timeout page at [`../sphinx/watchdogs-and-timeouts.md`](../sphinx/watchdogs-and-timeouts.md). The timeout-and-watchdog mental model, the workspace-change-weight table, the per-phase and per-iteration timeouts, the MCP timeout contract, and the recovery budget all live on that single canonical page.
