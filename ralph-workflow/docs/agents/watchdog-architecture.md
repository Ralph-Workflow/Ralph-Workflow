# Watchdog architecture (moved)

This material has been consolidated into the canonical watchdog and timeout page at [`../sphinx/watchdogs-and-timeouts.md`](../sphinx/watchdogs-and-timeouts.md). The watchdog architecture itself is described in conceptual form on the same page.
